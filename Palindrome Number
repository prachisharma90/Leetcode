bool isPalindrome(long x) {
      long s=0;
    int o=x;
    if(x<0)
    false;
   while(x>0) {
       
       s= s*10+x%10;
        x=x/10;

    }
    if(o==s)
    return true;
    else 
    return false;
}   
    
